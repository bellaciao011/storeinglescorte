KIT BASICO

<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Checkout Embed</title>
  <style>
    body { margin: 0; padding: 0; }
    iframe { width: 100%; height: 100vh; border: 0; }
  </style>
</head>
<body>
  <iframe
    id="cooud-checkout-frame"
    src="https://checkout.cooud.com/embed/01KS137170J22EER29WXMDJ5T7"
    allow="payment; storage-access"
    scrolling="auto">
  </iframe>
  <script>
    (function () {
      var frame = document.getElementById('cooud-checkout-frame');
      window.addEventListener('message', function (event) {
        if (event.origin !== "https://checkout.cooud.com") return;
        if (frame && event.source !== frame.contentWindow) return;

        var data = event.data || {};
        if (data._cooud !== true || data.v !== 1) return;

        if (data.type === 'cooud:redirect' && typeof data.url === 'string') {
          window.location.replace(data.url);
        }
      });
    })();
  </script>
</body>
</html>


KIT INICIANTE

<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Checkout Embed</title>
  <style>
    body { margin: 0; padding: 0; }
    iframe { width: 100%; height: 100vh; border: 0; }
  </style>
</head>
<body>
  <iframe
    id="cooud-checkout-frame"
    src="https://checkout.cooud.com/embed/01KS14367WFPP7TQ98HS2ACAC5"
    allow="payment; storage-access"
    scrolling="auto">
  </iframe>
  <script>
    (function () {
      var frame = document.getElementById('cooud-checkout-frame');
      window.addEventListener('message', function (event) {
        if (event.origin !== "https://checkout.cooud.com") return;
        if (frame && event.source !== frame.contentWindow) return;

        var data = event.data || {};
        if (data._cooud !== true || data.v !== 1) return;

        if (data.type === 'cooud:redirect' && typeof data.url === 'string') {
          window.location.replace(data.url);
        }
      });
    })();
  </script>
</body>
</html>


KIT CAMPEON


<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Checkout Embed</title>
  <style>
    body { margin: 0; padding: 0; }
    iframe { width: 100%; height: 100vh; border: 0; }
  </style>
</head>
<body>
  <iframe
    id="cooud-checkout-frame"
    src="https://checkout.cooud.com/embed/01KS14A4TGAGPQSBXAMRAKFXHY"
    allow="payment; storage-access"
    scrolling="auto">
  </iframe>
  <script>
    (function () {
      var frame = document.getElementById('cooud-checkout-frame');
      window.addEventListener('message', function (event) {
        if (event.origin !== "https://checkout.cooud.com") return;
        if (frame && event.source !== frame.contentWindow) return;

        var data = event.data || {};
        if (data._cooud !== true || data.v !== 1) return;

        if (data.type === 'cooud:redirect' && typeof data.url === 'string') {
          window.location.replace(data.url);
        }
      });
    })();
  </script>
</body>
</html>



Kit Coleccionista

<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Checkout Embed</title>
  <style>
    body { margin: 0; padding: 0; }
    iframe { width: 100%; height: 100vh; border: 0; }
  </style>
</head>
<body>
  <iframe
    id="cooud-checkout-frame"
    src="https://checkout.cooud.com/embed/01KS14E2ENQY9PHF9E446W3FA9"
    allow="payment; storage-access"
    scrolling="auto">
  </iframe>
  <script>
    (function () {
      var frame = document.getElementById('cooud-checkout-frame');
      window.addEventListener('message', function (event) {
        if (event.origin !== "https://checkout.cooud.com") return;
        if (frame && event.source !== frame.contentWindow) return;

        var data = event.data || {};
        if (data._cooud !== true || data.v !== 1) return;

        if (data.type === 'cooud:redirect' && typeof data.url === 'string') {
          window.location.replace(data.url);
        }
      });
    })();
  </script>
</body>
</html>



Álbum Golden Edition

<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Checkout Embed</title>
  <style>
    body { margin: 0; padding: 0; }
    iframe { width: 100%; height: 100vh; border: 0; }
  </style>
</head>
<body>
  <iframe
    id="cooud-checkout-frame"
    src="https://checkout.cooud.com/embed/01KS14HQMQDCP6KX7NRXYRTMPG"
    allow="payment; storage-access"
    scrolling="auto">
  </iframe>
  <script>
    (function () {
      var frame = document.getElementById('cooud-checkout-frame');
      window.addEventListener('message', function (event) {
        if (event.origin !== "https://checkout.cooud.com") return;
        if (frame && event.source !== frame.contentWindow) return;

        var data = event.data || {};
        if (data._cooud !== true || data.v !== 1) return;

        if (data.type === 'cooud:redirect' && typeof data.url === 'string') {
          window.location.replace(data.url);
        }
      });
    })();
  </script>
</body>
</html>



Kit Estadio — Edición Limitada


<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Checkout Embed</title>
  <style>
    body { margin: 0; padding: 0; }
    iframe { width: 100%; height: 100vh; border: 0; }
  </style>
</head>
<body>
  <iframe
    id="cooud-checkout-frame"
    src="https://checkout.cooud.com/embed/01KS14MTF1T7YY2S7QNXE7HMDS"
    allow="payment; storage-access"
    scrolling="auto">
  </iframe>
  <script>
    (function () {
      var frame = document.getElementById('cooud-checkout-frame');
      window.addEventListener('message', function (event) {
        if (event.origin !== "https://checkout.cooud.com") return;
        if (frame && event.source !== frame.contentWindow) return;

        var data = event.data || {};
        if (data._cooud !== true || data.v !== 1) return;

        if (data.type === 'cooud:redirect' && typeof data.url === 'string') {
          window.location.replace(data.url);
        }
      });
    })();
  </script>
</body>
</html>